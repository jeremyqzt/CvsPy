#include <math.h>
#include <stdlib.h>

int sieve(int limit){
	int ret = 0;
	int computationalLimit = floor(sqrt(limit));

	char *prime = malloc(limit + 1);

	for (int i = 2; i < computationalLimit; i++){
		
	}

	free(prime);
	return ret;
}
